# Intro

Simple repo for testing Git/GitHub workflows. Contains a small Python file `hello.py`.

## Files

- `hello.py` — example script

## Usage

No dependencies. Run:

```bash
python3 hello.py
```
